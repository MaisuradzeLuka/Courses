import { SingleCourseType } from "@/types";

type Props = {
  data: SingleCourseType;
  token: string | null;
};

const EnrollmentData = ({ data, token }: Props) => {
  return <section className="col-span-3">fe</section>;
};

export default EnrollmentData;
